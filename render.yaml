
services:
  - type: worker     # ← aquí está el cambio importante
    name: telegram-bot
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
